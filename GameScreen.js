import React from 'react';
import {Text, View} from 'react-native';

function GameScreen({navigation}) {
    return (
        <View style={{flex:1, justifyContent: 'center', alignItems:'center'}}>
            <Text>Game Screen :D</Text>
        </View>
    );
}

export default GameScreen;